{
  "name": "@personaxis/evals",
  "version": "0.12.0",
  "description": "Governance/property eval harness for Personaxis, proves the clamp holds, the gate blocks, memory stays tamper-evident, budgets stop, and verification catches. Deterministic (no API key needed).",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "personaxis-evals": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "eval": "node dist/index.js"
  },
  "dependencies": {
    "@personaxis/core": "workspace:*",
    "@personaxis/spec": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/personaxis/personaxis.git",
    "directory": "packages/evals"
  }
}
